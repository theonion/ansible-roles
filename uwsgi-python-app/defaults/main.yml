---
# Defaults for python uwsgi apps
virtualenv_dir: "/var/venvs/{{ app_name }}"
source_dir: "/www/{{ app_name }}"
app_enabled: False
appuser:
  name: "{{ app_name }}"
  home: "{{ source_dir }}"
system_packages: []
python_packages: []
authorized_keys: []
env_vars: []